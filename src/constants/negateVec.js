//---------------------------------------------------------
// Purpose: negates an array
//---------------------------------------------------------
const negateVec = (array) =>
{
  return array.map(elm => elm*(-1))
}

export default negateVec;